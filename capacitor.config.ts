import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.tannersterminal',
  appName: 'tannersterminal',
  webDir: 'dist'
};

export default config;
